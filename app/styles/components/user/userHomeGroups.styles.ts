import { ScrollViewProps } from "react-native";
import styled from "styled-components/native";

export const GroupListScrollView = styled.ScrollView<ScrollViewProps>`
  width: 100%;
  max-height: 100%;
  padding: 6px;
`;
