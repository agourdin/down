import { ScrollViewProps } from "react-native";
import styled from "styled-components/native";
import { colors } from "../../global.styles";

export const UserInvitesContainer = styled.ScrollView`
  background-color: transparent;
`;
